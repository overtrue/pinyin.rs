# Pinyin.rs Cursor 规则文件

## 项目概述
这是一个 Rust 实现的中文拼音转换库，支持汉字、词汇、姓氏的拼音转换。

## 代码风格规范

### Rust 代码风格
- 使用 `rustfmt` 标准格式化
- 函数名使用 snake_case
- 结构体和枚举使用 PascalCase
- 常量使用 SCREAMING_SNAKE_CASE
- 模块名使用 snake_case
- 行长度限制为 100 字符

### 命名约定
- 加载器类型以 `Loader` 结尾 (如 `WordsLoader`, `CharsLoader`)
- 错误类型以 `Error` 结尾
- 匹配器类型以 `Matcher` 结尾
- 静态全局变量使用 SCREAMING_SNAKE_CASE

### 注释规范
- 公共 API 必须有文档注释 (`///`)
- 复杂逻辑需要行内注释
- 中文注释优先，英文注释作为补充
- 构建脚本中的数据处理步骤需要详细注释

## 架构模式

### 模块组织
```
src/
├── lib.rs          # 主入口，公共 API
├── loader.rs       # 数据加载器
├── matcher.rs      # 拼音匹配器
├── pinyin.rs       # 拼音转换核心逻辑
└── error.rs        # 错误定义
```

### 数据流架构
1. 构建时：`sources/` → `build.rs` → `data/`
2. 运行时：`data/` → `Loader` → `Matcher` → 拼音结果

### 并发安全
- 使用 `OnceLock` 实现线程安全的单例模式
- 使用 `rayon` 进行并行数据处理
- 所有公共 API 必须是线程安全的

## 数据处理规范

### 构建脚本 (build.rs)
- 数据生成函数命名：`generate_*`
- 数据解析函数命名：`parse_*`
- 必须处理所有可能的错误情况
- 分块策略：大数据集分为 10 个文件
- 数据优先级：补丁数据 > 原始数据

### 数据格式
- 字符数据：`字符: 拼音`
- 词汇数据：`词汇: 拼音`
- Unicode 格式：`U+4E00: 拼音 # 注释`
- 多音字：逗号分隔列表

### 数据验证
- 所有输入数据必须验证格式
- 拼音必须符合标准格式
- Unicode 码点必须有效
- 空行和注释行正确处理

## 性能优化规范

### 内存管理
- 使用 `Vec` 和 `HashMap` 进行数据存储
- 避免不必要的字符串克隆
- 使用 `&str` 而不是 `String` 进行临时操作

### 并行处理
- 使用 `rayon::par_iter()` 进行并行匹配
- 大数据集处理必须支持并行
- 避免在并行代码中使用共享可变状态

### 缓存策略
- 使用 `OnceLock` 实现延迟初始化
- 预编译数据嵌入二进制文件
- 避免运行时重复加载数据

## 测试规范

### 单元测试
- 每个公共函数必须有测试
- 测试用例包含边界情况
- 使用 `pretty_assertions` 进行断言
- 性能测试使用 `std::time::Instant`

### 测试数据
- 测试用例覆盖常见汉字、词汇、姓氏
- 包含多音字测试
- 包含特殊字符和标点符号
- 测试数据应该具有代表性

### 基准测试
- 关键路径必须有性能测试
- 记录加载时间和转换时间
- 使用 `#[cfg(test)]` 条件编译性能代码

## 错误处理

### 错误类型
- 使用 `thiserror` 定义错误类型
- 错误信息要清晰明确
- 区分可恢复和不可恢复错误

### 错误传播
- 使用 `Result<T, E>` 进行错误传播
- 构建脚本使用 `anyhow` 处理错误
- 避免 `panic!`，除非是不可恢复的错误

## 依赖管理

### 核心依赖
- `daachorse`: 字符串匹配算法
- `rayon`: 并行处理
- `thiserror`: 错误处理

### 开发依赖
- `pretty_assertions`: 测试断言
- `anyhow`: 构建脚本错误处理

### 版本策略
- 使用语义化版本
- 定期更新依赖
- 避免引入不必要的依赖

## 文档规范

### API 文档
- 所有公共 API 必须有文档注释
- 包含使用示例
- 说明参数和返回值
- 标注可能的错误情况

### README 文档
- 包含安装说明
- 提供使用示例
- 说明项目特性
- 包含贡献指南

### 代码示例
- 示例代码必须可运行
- 覆盖主要使用场景
- 包含错误处理示例

## 构建和发布

### 构建配置
- 使用 `build.rs` 进行数据预处理
- 支持增量构建
- 构建产物包含预编译数据

### 版本发布
- 遵循语义化版本规范
- 更新 CHANGELOG
- 标记重要的 breaking changes

### CI/CD
- 自动化测试和构建
- 代码格式检查
- 性能回归测试

## 特殊规则

### 中文处理
- 正确处理 Unicode 字符
- 支持繁简体字符
- 处理多音字和异读字

### 拼音格式
- 使用标准拼音格式（带声调）
- 支持多种拼音输出格式
- 保持拼音的准确性

### 数据更新
- 支持数据补丁机制
- 补丁数据优先级高于原始数据
- 提供数据验证工具

## 代码审查检查清单

### 功能性
- [ ] 新功能是否有对应测试
- [ ] 是否处理了边界情况
- [ ] 错误处理是否完整
- [ ] 性能是否符合要求

### 代码质量
- [ ] 代码风格是否一致
- [ ] 命名是否清晰
- [ ] 注释是否充分
- [ ] 是否有代码重复

### 安全性
- [ ] 是否有内存安全问题
- [ ] 并发安全是否正确
- [ ] 输入验证是否充分

### 维护性
- [ ] 代码结构是否清晰
- [ ] 模块职责是否单一
- [ ] 依赖关系是否合理
